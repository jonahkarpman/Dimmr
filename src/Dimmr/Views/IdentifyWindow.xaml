<Window x:Class="Dimmr.Views.IdentifyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#CC0A0A0A"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        ShowActivated="False"
        Focusable="False"
        WindowStartupLocation="Manual">
    <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="4" Margin="60">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="NumberText" FontFamily="Consolas" FontSize="200"
                       Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" />
            <TextBlock x:Name="LabelText" FontFamily="Consolas" FontSize="26"
                       Foreground="{StaticResource PrimaryTextBrush}" HorizontalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
