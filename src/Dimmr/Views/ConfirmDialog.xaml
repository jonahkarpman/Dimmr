<Window x:Class="Dimmr.Views.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CONFIRM"
        Width="380" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        AutomationProperties.Name="Confirmation dialog">
    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="2" Padding="20">
        <StackPanel>
            <TextBlock x:Name="TitleText" Foreground="{StaticResource DangerBrush}"
                       FontFamily="Consolas" FontSize="15" FontWeight="Bold" Margin="0,0,0,12" />
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="[ ABORT ]" Click="OnCancel" IsCancel="True" Margin="0,0,8,0"
                        AutomationProperties.Name="Abort" />
                <Button Content="[ AFFIRM ]" Click="OnConfirm" IsDefault="True"
                        Foreground="{StaticResource DangerBrush}"
                        AutomationProperties.Name="Affirm" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
